<%# Timeline %>
<div class="d-flex">
  <div class="timeline-<%= status == 'payed' ? 'orange' : 'green' %>">
    <div class="container-timeline-green">
    </div>
  </div>
  <div class="timeline-<%= status == 'payed' ? 'orange' : 'green'%>">
    <div class="container-timeline-<%= status == 'payed' ? 'green' : 'orange'%>">
    </div>
  </div>
  <div class="timeline-green-last">
    <div class="container-timeline-<%= status == 'payed' ? 'green' : 'orange'%>">
    </div>
  </div>
</div>
